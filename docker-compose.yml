services:
  remote-hub:
    build: .
    container_name: remote-hub
    restart: always
    ports:
      - "3080:3080"
    volumes:
      - ./server/users.db:/app/server/users.db
    environment:
      - JWT_SECRET=supersecretkey-remote-pc
      - PORT=3080
